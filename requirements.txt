# Runtime dependencies verified against the main entry points.
# For CUDA training, install torch and torchvision from the matching official wheel first.

torch>=2.5,<2.6
torchvision>=0.20,<0.21
einops>=0.8
timm>=1.0
pyyaml>=6.0
opencv-python>=4.8
numpy>=1.26
matplotlib>=3.8
tensorboard>=2.16
tqdm>=4.66
